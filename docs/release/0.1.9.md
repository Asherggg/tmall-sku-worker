# Tmall SKU Worker v0.1.9

## 变更内容

- 任务队列支持手动删除任务。
- 未开始执行的任务可以删除，所属批次会同步更新；批次没有剩余任务时自动移除。
- 删除操作会保留审计记录，不会删除审计导出中的删除事件。
- 正在执行的任务不能删除。
- 已进入线上写入但尚未确认结果的任务不能删除，避免掩盖未决写入。
- 版本号按项目规则从 `0.1.8` 升级到 `0.1.9`。

## 使用方式

在“任务队列”中，点击任务行的垃圾桶按钮，确认后即可删除。也可以先打开任务详情，再点击“删除任务”。
