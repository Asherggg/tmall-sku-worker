# Tmall SKU Worker v0.1.11

## 变更内容

- 桌面界面只提供线上运行模式，移除演练/线上分段选择器。
- 创建线上批次不再要求输入确认词。
- 点击“创建线上批次”后仍显示一次明确的线上写入确认对话框，防止误操作。
- 批次预览最多展开前 10 个商品；超过 10 个时显示剩余数量，不再无限拉长窗口。
- Worker 内部仍保留线上确认契约和演练测试能力，但安装版用户界面不再暴露演练入口。
