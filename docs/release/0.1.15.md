# Tmall SKU Worker v0.1.15

## 变更

- 最终回读最多执行 12 次；临时回读继续保持 6 次，避免平台最终数据延迟造成误报。
- 增加最终回读在第 8 次才收敛的自动化用例。
- 保留 v0.1.14 的多详情结构、SKU 明细和自定义销售属性异步校验能力。

## 真实验证

- 商品 `969100931827` 已完成两阶段提交，旧 SKU `6289425475079-5082` 已替换为新 SKU `6291080454140-4143`。
- 首轮最终回读在 12 秒窗口内误报字段不一致；稍后直接服务端回读确认全部非库存业务字段与原快照一致。
- 剩余两个商品将在安装 v0.1.15 后继续串行验证。
