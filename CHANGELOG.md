# Changelog

本文件记录天猫运营工作台及其应用版本更新。详细发布验证见 [`docs/release/`](docs/release/)。

## [0.1.31] - 2026-09-23

### Worker 重启恢复

- 修复 Worker 重启后持久化任务状态与内存调度器脱离，导致任务显示持续活动但无法启动或删除的问题。
- Worker 启动时扫描 `queued` 和执行阶段任务；未自动恢复执行，统一进入 `needs_manual_review`，并写入重启恢复审计记录。
- 已发出天猫写入的任务保留线上写入锁，必须先人工核对服务端状态；不能自动重试。
- 未发出写入的中断任务可以在确认未执行后按现有人工流程重试或删除。
- 批次状态会根据恢复后的任务状态重新计算，避免继续显示不存在的内存调度。

### 验证与打包

- 新增 Worker 重启恢复、禁止自动重跑和人工删除回归测试。
- `npm test`：81/81 passed。
- `npm run build`：passed；`cargo check`、`cargo test` 和生产依赖审计沿用当前发布基线。
- NSIS 与 MSI 安装包均已生成，版本表面与打包 Worker 均为 `0.1.31`。
- 安装包为本地未签名构建，安装不会自动覆盖当前安装目录。

## [0.1.30] - 2026-09-22

### 旧销售渠道迁移

- 将旧 `channelOption=5` 或空值分类为 `channel_option_migration_required`，在任何销售属性预检或商品提交 POST 前停止；不再把旧值加入白名单，也不静默映射。
- 任务队列和详情新增销售渠道选择入口，只为明确等待迁移且尚未发出线上写入的单任务提供 `1`（纯电商）与 `2`（商场同款）。
- 重试请求必须同时携带任务级选择和显式确认；Worker 重新读取当次 bootstrap，核对旧值未变化且所选值仍由页面唯一提供后才继续。
- 已合法的 `1/2` 保持原值，环境变量和新建批次参数不能覆盖；人工选择后的最终回读严格核对所选渠道。
- 兼容 v0.1.28 已保存的 `channel_option_invalid` 失败任务；已发出或结果未知的写入仍禁止重跑。

### 验证

- 自动化覆盖旧值 `5`、空值、合法 `1/2`、显式选择、组件候选漂移、零写入阻断和最终渠道回读；Node 基线增至 76 项。
- Vue/TypeScript、Vite 生产构建、Rust 检查和生产依赖审计通过；NSIS 与 MSI `0.1.29` 安装包构建完成。
- 迁移弹窗和任务详情在 `1440×900`、`1040×680` 完成 Playwright 验收，无溢出或控制台错误。
- 已从现有 `0.1.28` 完成真实原地升级、只读登录验证和完整回滚；安装文件清单与注册表恢复一致，专属 Edge/Profile 保持运行。

## [0.1.28] - 2026-09-21

### Windows 升级修复

- 修复旧 Worker 子进程异常残留时，NSIS 升级无法覆盖 `runtime\\node.exe` 并显示“Error opening file for writing”的问题。
- 新增 NSIS `NSIS_HOOK_PREINSTALL` / `NSIS_HOOK_PREUNINSTALL`：仅按目标安装目录下的完整 Node 可执行文件路径结束对应 Worker，不影响其他 Node 进程、Edge 进程或专属 Profile。
- 继续保留 `Tmall SKU Worker` 技术包身份，安装目录、应用数据和登录 Profile 可原地升级。
- 已在旧 Worker 正持有 `runtime\\node.exe` 的真实安装目录上验证升级退出状态为 0，锁定进程自动结束。
- Tauri host 将 bundled Node Worker 放入带 `JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE | JOB_OBJECT_LIMIT_SILENT_BREAKAWAY_OK` 的 Windows Job Object；宿主正常退出或异常关闭时，Node 不会遗留为孤立进程，Worker 后续启动的专属 Edge 则自动脱离该 Job 并保持运行。

## [0.1.27] - 2026-09-20

### 新增花型应用

- 新增独立 `新增花型` 应用窗口，支持 `.xlsx` 选择/拖放、八列严格校验、按商品分组预览、线上确认、任务队列、详情时间线和浏览器会话。
- Excel 每行仅代表一个明确的 `规格 + 颜色分类` 组合；备注“原花型不增加新规格”不丢弃该行，也不会生成未列出的笛卡尔积组合。
- 保留所有线上 SKU 和原 SKU ID；已存在组合只读核对价格、商家编码和条码，缺少组合才新增。全为已存在组合时零写入，有新增时每个商品最多一次提交。
- 新组合必须有商家编码，且商家编码/条码不能与线上或批内其他行冲突；销售属性映射、模板和必填元数据不唯一时写入前阻断。
- 提交继续使用全局串行写锁、150 秒严格最终回读、未知结果人工复核和已发出写入禁止自动重试。

### 工作台与质量

- 工作台扩展为四个桌面入口和两个持久应用实例；SKU 草稿与新增花型 Excel 预览在窗口关闭、最小化和任务栏切换后保持。
- ExcelJS 只进入前端构建，不进入 Worker 资源；`uuid` 传递依赖固定到已修复版本，生产依赖审计为 0。
- 自动化基线扩展到 68 项，并用实际 8 商品、23 行样表完成 `1440×900` 与 `1040×680` Playwright 验收。

## [0.1.26] - 2026-09-20

### 运营工作台外壳

- 启动页改为“天猫运营工作台”桌面；SKU 重建不再占用整个产品入口，而是作为首个应用模块运行。
- 增加 `SKU ID 重建`、`任务中心`、`浏览器会话`桌面快捷方式，支持双击与键盘 Enter 启动。
- 增加工作台内部应用窗口，支持拖动、调整大小、最小化、最大化、还原和关闭；隐藏窗口不会卸载模块或停止后台 Worker 任务。
- 增加底部任务栏、运行实例切换、Worker/登录全局状态，以及运行任务和待复核数量徽标。
- 保留原有两阶段 SKU 重建、最多两路商品流水线、Tmall 写请求串行、150 秒严格回读和人工复核边界。

## [0.1.25] - 2026-09-02

### 激进批处理性能

- 同一线上批次最多并行处理 2 个不同商品；每个商品内部仍严格执行快照、临时提交/回读、恢复提交和最终回读。
- 所有 `/tmall/submit.htm` 写请求通过 Worker 级写锁串行，同一商品仍受未确认写入锁保护；未知结果不自动重试。
- 最终回读采用 500ms、1s、2s、4s、8s 后封顶 10s 的自适应间隔，150 秒硬截止和截止点新鲜 GET 保持不变。
- 只读发布页回读可在服务端模型结束处停止接收响应；流式路径解析失败时自动回退到完整的 BrowserContext GET。
- 阶段耗时、轮询次数、传输策略和截止状态写入脱敏审计，未记录 Cookie、Token 或完整请求体。

## [0.1.24] - 2026-08-25

### 纯 SKU ID 重建

- 线上任务只执行天猫商品快照、临时规格提交、新 SKU ID 回读、原业务字段恢复和最终回读。
- 移除 OMS、外部资料查询与条码补全、国补流程及对应登录入口和 Worker API。
- 安装资源只包含核心 Worker、天猫适配器、Node 和 Playwright，不再携带流程凭据、ExcelJS 或 MySQL 客户端。
- 最终校验继续严格比较规格、价格、商家编码、条码、图片等原快照字段；库存采用平台实时值。

## [0.1.23] - 2026-08-21

### 固定工作流

- 商品资料服务固定为 `http://10.21.16.213:9031/v1/materials/lookup`，运行时配置不能覆盖。
- 国补流程固定开启，桌面 UI 不再显示流程配置入口。
- 访问凭据由发布构建生成到忽略提交的安装资源，Tauri 启动 Worker 时直接注入；用户只需完成三个站点登录。
- 发布构建缺少固定凭据时直接失败，避免生成无法执行线上批次的安装包。

## [0.1.22] - 2026-08-21

### 流程配置

- 新增应用内“流程配置”页，可填写 Doris 商品资料服务地址、查询路径和访问令牌，并启用国补流程。
- Token 只写入用户应用数据目录；保存后前端只能看到“已配置”状态，不会回显 Token。
- Tauri host 在首次启动时迁移显式旧配置路径、旧一级目录配置或受支持的旧环境变量，覆盖升级保留现有配置。
- Worker `/health` 返回精确缺失项，不再把三站登录成功误写成 OMS、Doris、SKU 重建和国补均未配置。

### 验证

- `npm test`: 63/63 passed。
- `npm run build`: passed。
- `cargo test --manifest-path src-tauri/Cargo.toml`: 2/2 passed。
- `cargo check --manifest-path src-tauri/Cargo.toml`: passed。

## [0.1.21] - 2026-08-21

### 国补纯接口

- 默认复用已登录专属 Edge Profile 的 `BrowserContext.request`，通过动态 MTop 签名完成模板导出、OSS 上传配置、文件导入和商品列表回读。
- `IMPORT` 保留外部写入门禁且不自动重试；响应未知或回读不一致进入人工复核。
- 提交后严格比对新 SKU ID、69 码、国补品名和规格；页面驱动流程保留为 `TMALL_SUBSIDY_TRANSPORT=page` 显式 fallback。
- 商品 `1061776009736` 已完成真实纯接口 `IMPORT`，提交只发送一次，服务端首次列表回读严格确认 9 个 SKU ID、69 码、国补品名和规格全部一致。

## [0.1.20] - 2026-08-20

### 国补流程

- 兼容当前国补页面的“手动输入商品 ID -> 下载模板 -> 上传 XLSX -> 提交”流程。
- 保留旧版商品 ID/替换 SKU ID 流程兼容分支。
- 上传完成后才允许进入最终提交；模板映射不完整、H/I/O/P/Q 填充不完整或上传未完成时停止。
- 实测商品 `1061776009736`：9 条 SKU 映射、9 行模板填充、最终提交和提交后列表回读全部成功。

### 运行时与配置

- 修复捆绑 ExcelJS 的 `readable-stream` 版本冲突，安装包可以读取真实国补 XLSX 模板。
- 通过应用数据目录 `runtime-config.json` 持久化远程资料 API 配置，不依赖 Explorer 的旧环境变量。
- 保留 150 秒最终回读截止时间和淘宝/OMS/国补三页面登录验证。

### 验证

- `npm test`: 56/56 passed。
- `npm run build`: passed。
- `cargo check --manifest-path src-tauri/Cargo.toml`: passed。
- NSIS 安装包：`Tmall SKU Worker_0.1.20_x64-setup.exe`。

## [0.1.19] - 2026-08-20

- 增加应用数据目录运行时配置文件和远程商品资料 API 支持。
- 修复安装版运行时配置无法从 Explorer 环境继承的问题。

## [0.1.18] - 2026-08-20

- 最终回读扩展到 150 秒截止窗口。
- 登录检查同时验证淘宝、OMS 和国补页面。

## [0.1.17] - 2026-08-20

- 增加 OMS 商品禁用、Doris 料号补全和国补模板工作流。
- 增加 SKU 明细页和自定义销售属性兼容。

## [0.1.16] - 2026-08-17

- 兼容多种商品详情页和 SKU 预检组合格式。
